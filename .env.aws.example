# AWS Deployment Environment Variables
# Copy this file to .env.aws and fill in the values after deploying infrastructure

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Option 1: Direct connection string (get from CDK outputs after deployment)
DATABASE_URL=postgresql://username:password@cluster-endpoint.region.rds.amazonaws.com:5432/vcmuellheim

# Option 2: Use AWS Secrets Manager (recommended for production)
# DATABASE_SECRET_ARN=arn:aws:secretsmanager:region:account:secret:vcmuellheim-env-db-credentials
# DATABASE_ENDPOINT=cluster-endpoint.region.rds.amazonaws.com
# DATABASE_PORT=5432
# DATABASE_NAME=vcmuellheim
# AWS_REGION=eu-central-1

# =============================================================================
# S3 STORAGE CONFIGURATION
# =============================================================================
# S3 bucket name (from CDK StorageStack outputs)
S3_BUCKET=vcmuellheim-dev-media
S3_REGION=eu-central-1

# S3 credentials (from CDK StorageStack outputs)
S3_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
S3_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# PAYLOAD CMS CONFIGURATION
# =============================================================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PAYLOAD_SECRET=your-secure-random-string-here
PAYLOAD_CONFIG_PATH=./payload.config.ts

# Development auto-login (dev environment only)
# PAYLOAD_DEV_EMAIL=admin@example.com
# PAYLOAD_DEV_PASSWORD=your-secure-password

# =============================================================================
# EMAIL CONFIGURATION (Optional)
# =============================================================================
# Resend API key for email functionality
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# ERROR TRACKING (Optional)
# =============================================================================
# Sentry configuration
# SENTRY_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# SENTRY_ENVIRONMENT=production

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
TZ=Europe/Berlin

# SAMS Integration
SAMS_SERVER=https://www.volleyball-baden.de

# Custom image loader (if using Coolify or custom CDN)
# COOLIFY_URL=https://your-custom-cdn.example.com

# =============================================================================
# AWS CREDENTIALS (for CDK deployment only - not needed in Amplify)
# =============================================================================
# Set these when deploying CDK infrastructure
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=eu-central-1

# AWS Profile (if using AWS CLI profiles)
# AWS_PROFILE=vcmuellheim

# =============================================================================
# NOTES
# =============================================================================
# 1. After deploying the CDK infrastructure, retrieve values from CloudFormation outputs:
#    - Database endpoint: aws cloudformation describe-stacks --stack-name VCMuellheimDevDatabaseStack
#    - S3 bucket: aws cloudformation describe-stacks --stack-name VCMuellheimDevStorageStack
#
# 2. Retrieve database credentials:
#    - Run: cd infrastructure && ./scripts/get-db-credentials.sh dev
#
# 3. For Amplify Hosting, set these in AWS Amplify Console under Environment Variables
#
# 4. For local development, copy this file to .env.local and update values
#
# 5. Never commit actual credentials to git!
