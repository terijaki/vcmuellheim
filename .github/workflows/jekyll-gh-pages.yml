name: Deploy Jekyll with GitHub Pages

on:
  push:
    branches:
      - "main"
    paths-ignore:
      - ".github/workflows/**"
      - "_sams/**"

  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # Use ImageMagick (bash: convert) to create thumbnail images dynamically
      - name: Create Thumbnails
        run: |
          for file in $(find upload/* -type f)
            do
              if [[ $file == *.jpg ]] || [[ $file == *.jpeg ]] || [[ $file == *.png ]]
                then
                  echo "Processing: "$file
                  
                  news_root=thumbnails/news
                  gallery_root=thumbnails/gallery
                  
                  news_folder=$news_root/${file%/*}
                  gallery_folder=$gallery_root/${file%/*}
                  
                  mkdir -p $news_folder
                  mkdir -p $gallery_folder

                  convert "$file" -thumbnail '680x160^' -quality 70 -gravity center -extent '680x160' $news_root/$file
                  convert "$file" -thumbnail '260x200^' -quality 50 -gravity center -extent '260x200' $gallery_root/$file
                fi
            done

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1