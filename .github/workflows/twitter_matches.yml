name: Twitter Match Announcements

on:
  workflow_dispatch:
  push:
    paths:
      - _spielergebnisse/**

env:
  TWITTER_OAUTH_SIGNATURE: ${{ secrets.TWITTER_OAUTH_SIGNATURE }}

jobs:
  New-Matches-to-Twitter:
    runs-on: macos-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3
        with: # important to ensure fetch all history!
          fetch-depth: 0

      - name: Setup Git Config
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"

      - name: Create placeholder variable
        run: declare matches_to_post=''

      - name: Scan Matches
        id: scanmatches
        run: |
          cd _spielergebnisse 
          for f in $(ls | tr '\n' '\n')
            do
              twitterhistory=$(git log --follow --pretty=oneline $f | grep 'twitter')
              if [ -z "$twitterhistory" ]
                then
                  echo "$f added to list of matches to post"
                  declare matches_to_post="$matches_to_post $f"
                else
                  echo "$f already posted on Twitter"
              fi
            done
          echo "The following matches have not been posted: $matches_to_post"
          echo "::set-output name=matches-to-post::$matches_to_post"

      - name: Mark matchfiles with string #adds a string to the end of the file name to indicate that it has been posted
        id: markfiles
        if: steps.scanmatches.outputs.matches-to-post != ''
        run: |
          git pull --quiet
          for matchfile in $matches_to_post
           do
            echo "TWITTERTOUCH" >> $matchfile
            git add "$matchfile"
          done
          git commit -m "tagged before posting on twitter"

      - name: Post to Twitter # post the lsat line in the match file to Twitter
        id: posttotwitter
        if: steps.markfiles.outcome == 'success'
        run: |
          echo 'Posting the following to Twitter: $matchfile'
          for matchfile in $matches_to_post
           do
            filetopost='$matchfile'
            message='$(tail -n 1 $filetopost)'
            curl -X POST "https://api.twitter.com/2/tweets" -H "Authorization: OAuth $TWITTER_OAUTH_SIGNATURE" -H "Content-type: application/json" -d '{"text":"$message"}'
          done

      - name: Remove matchfiles mark #removes the temporary string
        id: removemark
        if: steps.posttotwitter.outcome == 'success'
        run: |
          git pull --quiet
          for matchfile in $matches_to_post
           do
            sed -i -e 's/TWITTERTOUCH//' $matchfile
            git add "$matchfile"
          done
          git commit -m "posted on twitter"
          git push origin --all --quiet
